DISCOVERY PHASE — Review Slice: {props.sliceId}

Instruction: {props.instruction}
Slice path: {props.slicePath}
Entry type: {props.entryType}
Pass: {props.passNumber}
Risk: {props.risk}

## Scope

Review only this bounded slice:
- {props.slicePath}

{props.inferredPaths && props.inferredPaths.length > 0
  ? `Related inferred paths:\n${props.inferredPaths.map((entry) => `- ${entry}`).join('\n')}`
  : ''}

## Focus Areas

{props.focusAreas && props.focusAreas.length > 0
  ? props.focusAreas.map((entry) => `- ${entry}`).join('\n')
  : '- bugs\n- simplification\n- architecture'}

## Slice Rationale

{props.rationale}

## Task

Discover candidate issues only. Do not write final tickets.

Each candidate must include:
- `candidateId`
- `dedupeKey`
- `kind`
- `priority`
- `confidence`
- `summary`
- `primaryFile`
- `lineRefs`
- `whyItMayMatter`

## Quality Bar

- Keep candidates grounded in concrete code evidence inside the slice.
- Use repo-relative paths in `primaryFile` and `lineRefs`.
- Make `dedupeKey` stable from kind + primary file + line anchor/symbol + normalized issue pattern.
- Return an empty `candidates` array if nothing survives the evidence bar.
- Set `inputSignature` to exactly `{props.inputSignature}`.
